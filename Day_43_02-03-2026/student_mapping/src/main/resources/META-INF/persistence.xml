<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             version="3.0">

    <persistence-unit name="studentMappingPU">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entities managed by this persistence unit -->
        <class>com.example.Student</class>
        <class>com.example.Course</class>

        <properties>
            <!-- JDBC Configuration -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/jpa_demo" />
            <property name="jakarta.persistence.jdbc.user"     value="root" />
            <property name="jakarta.persistence.jdbc.password" value="system" />
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.mysql.cj.jdbc.Driver" />

            <!-- Hibernate Configuration -->
            <!-- 'update' creates/alters tables on startup without dropping data -->
            <property name="hibernate.hbm2ddl.auto" value="update" />
            <property name="hibernate.show_sql"      value="true" />
            <property name="hibernate.format_sql"    value="true" />
        </properties>
    </persistence-unit>

</persistence>
